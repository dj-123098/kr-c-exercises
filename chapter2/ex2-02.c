// ex2-02.c -- for sentence without && and ||

/*
----------------------------------ANSWER-----------------------------------
for (i = 0; ((i < lim1) + ((c = getchar()) != '\n') + (c != EOF)) == 3; ++i)
	s[i] = c;
*/

int main(void)
{
	return 0;
}